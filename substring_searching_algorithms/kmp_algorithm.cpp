#include <iostream>

using namespace std;

int KMPsubstringSearch(string str, string substr);

int KMPsubstringSearch(string str, string substr) {
    int M = str.length();
    int N = substr.length();



    return -1;
}